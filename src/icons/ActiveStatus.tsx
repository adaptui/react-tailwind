import { createIcon } from "../create-icon";

export const ActiveStatusIcon = createIcon({
  displayName: "ActiveStatusIcon",
  viewBox: "0 0 4 4",
  path: <circle cx="2" cy="2" r="2" fill="currentColor" />,
});
